        <nav class="navbar navbar-expand-lg navbar-warning bg-warning">
            <div class="container-fluid">
                <a class="navbar-brand text-white" href="{{ path('app_home') }}">MATERIA</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                {%  if is_granted('ROLE_ADMIN') %}
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active text-white" aria-current="page" href="">ajouter article</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link text-white" href="">liste user</a>
                        </li>
                    </ul>
                {% endif %}
                    <div class="dropdown mr-3">
                        <button class="btn btn-secondary dropdown-toggle bg-warning" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                            ADMIN
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            {% if not app.user %}
                            <li><a class="dropdown-item" href="">Insrcyption</a></li>
                            <li><a class="dropdown-item" href="{{path('app_login')}}">connexion</a></li>
    
                            {% else %}
    
                            <li><a class="dropdown-item" href="">deconexion</a></li>
                            <li><a class="dropdown-item" href="">mon Compte</a></li>
                           
                                
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </nav>